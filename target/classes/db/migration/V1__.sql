create table user (
id bigint UNSIGNED AUTO_INCREMENT PRIMARY KEY,
name varchar(50),email varchar(50),
   created_date date,updated_date date, created_by varchar(20),updated_by varchar(20)
    
);